# Exercises: Wrapper & Autoboxing
1. Benchmark sum using `Integer` vs `int` for 5M iterations.
2. Demonstrate cache effect: compare identity of 127 and 128 values.
3. Refactor code replacing boxing inside loops with primitive streams.
4. Implement safe unboxing utility with default value.
5. Write a microbenchmark measuring `Map<Integer,Integer>` vs `Int2IntMap` (if using fastutil) for large ranges.

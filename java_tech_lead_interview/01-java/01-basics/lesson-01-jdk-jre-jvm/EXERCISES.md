## Exercise 1
Goal: Inspect bytecode.
Run `javac HelloJVM.java` then `javap -c HelloJVM` and describe output.

## Exercise 2
Goal: Build custom runtime.
Use `jlink` with `java.base` only and run program.

## Exercise 3
Goal: Use `jdeps`.
Analyze dependencies of a small multi-class project.

## Exercise 4
Goal: Measure start time.
Compare `java -Xlog:startup` output for different flags.

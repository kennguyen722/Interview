# Exercises: String Immutability
1. Benchmark string concatenation using `+` vs `StringBuilder` for 10k iterations.
2. Show difference between pooled literal and `new String()` object.
3. Implement a method that safely builds a CSV row using `StringBuilder`.
4. Explore memory impact of interning many generated UUIDs.
5. Replace inefficient concatenations in sample code with `StringBuilder`.

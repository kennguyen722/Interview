# Quiz: String Immutability
1. Why are Strings immutable? Give two reasons.
2. Difference between `==` and `.equals` for strings?
3. Why use `StringBuilder` instead of `+` in loops?
4. What does `intern()` do?
5. Is `StringBuffer` still commonly needed? When?

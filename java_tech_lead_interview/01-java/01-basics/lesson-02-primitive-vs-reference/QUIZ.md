# Quiz: Primitive vs Reference Types
1. What does `==` compare for reference types?
2. Why can `Integer` cause NPE during unboxing?
3. Range of Integer cache by default?
4. Which is faster generally: primitive `int` or `Integer` in arithmetic loops?
5. How to safely compare possibly-null `Integer` values?

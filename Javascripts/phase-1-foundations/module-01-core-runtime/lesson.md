# Module 01: Core Runtime

## Goals
- Master scope, closures, and object behavior.
- Implement common interview helpers without libraries.
- Build a minimal event emitter.

## Starter Task
- Complete `groupBy`, `deepClone`, `flattenArray`, and `createEmitter` in `starter/index.js`.

## Validation
- Run the built-in smoke checks in each file with Node.

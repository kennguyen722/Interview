# Roku Mock Attempts Workspace

Implement your own answers here and validate with:

- `npm run test:roku-attempts`
- `npm run test:roku-attempts:easy`
- `npm run test:roku-attempts:medium`
- `npm run test:roku-attempts:hard`

Expected behavior initially:

- Tests fail at first because attempt files start with TODO stubs.
- As you implement each mock, corresponding tests should pass.

Suggested progression:

1. Pass `easy` first.
2. Then pass `medium`.
3. Finish with `hard` and finally `test:roku-attempts`.

## Files to implement

- `mock-01.js`
- `mock-02.js`
- `mock-03.js`
- `mock-04.js`
- `mock-05.js`

## Workflow

1. Pick one candidate prompt from `../mock-0X-candidate-prompt.md`.
2. Implement the corresponding attempt file in this folder.
3. Run `npm run test:roku-attempts`.
4. Compare with official solution only after first attempt:
   - `../solutions/mock-0X-solution.js`
   - `../solutions/mock-0X-explanation.md`
